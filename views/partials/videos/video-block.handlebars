{{!-- <li>
	{{name}}

	<button class="change-sleep" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if sleep}}SLEEP TIME!{{else}}WAKE UP!{{/if}}
	</button>

	<button class="delete-cat" data-id="{{this.id}}">DELETE!</button>
</li> --}}

    <div class="col s12 m6 l3">
      <a href="#{{@index}}" class="waves-effect waves-light modal-trigger">
          <div class="card darkModeOFF">
            <div class="card-image">
              <img src="{{this.thumbnails}}" alt="VideoBlock" onerror="this.onerror=null;this.src='images/image-not-found.jpg';">
              <a class="btn-floating halfway-fab waves-effect waves-light"><i class="material-icons">add</i></a>
            </div>
            <div class="card-content">
            {{!-- <span class="card-title">Youtube</span> --}}
              <p>{{title}}</p>
            </div>
          </div>
      </a>
    </div>
